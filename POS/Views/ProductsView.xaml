<UserControl x:Class="POS.Views.ProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000">
    <Grid Background="#F4F5F7">
        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Page Header Area -->
            <Grid Grid.Row="0" Margin="0,0,0,25">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="📦" FontSize="20" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <StackPanel>
                        <TextBlock Text="PRODUCTS" FontSize="20" FontWeight="SemiBold" Foreground="#111827"/>
                        <TextBlock Text="INVENTORY MANAGEMENT" FontSize="11" Foreground="#6B7280"/>
                    </StackPanel>
                </StackPanel>
            </Grid>

            <!-- Summary Dashboard Area -->
            <Grid Grid.Row="1" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <!-- Card 1: Total Product (formerly SKUs) -->
                <Border Grid.Column="0" Background="#4F46E5" CornerRadius="12" Padding="20,15" Margin="0,0,10,0">
                    <StackPanel>
                        <TextBlock Text="TOTAL SKUs" FontSize="10" FontWeight="Bold" Foreground="#A5B4FC"/>
                        <TextBlock Text="{Binding TotalSKUs}" FontSize="24" FontWeight="Bold" Foreground="White" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>
                <!-- Card 2: Total Inventory Units -->
                <Border Grid.Column="1" Background="White" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="12" Padding="20,15" Margin="10,0,0,0">
                    <StackPanel>
                        <TextBlock Text="TOTAL UNITS" FontSize="10" FontWeight="Bold" Foreground="#6B7280"/>
                        <TextBlock Text="{Binding TotalRemainingStock}" FontSize="24" FontWeight="Bold" Foreground="#111827" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Search Bar -->
            <Grid Grid.Row="2" Margin="0,0,0,20">
                <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Padding="15,12" Background="White" BorderBrush="#E5E7EB" BorderThickness="1" FontSize="14" HorizontalAlignment="Stretch">
                    <TextBox.Resources>
                        <Style TargetType="{x:Type Border}">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </TextBox.Resources>
                </TextBox>
            </Grid>

            <!-- Product Table Design -->
            <Border Grid.Row="3" Background="White" CornerRadius="12" BorderBrush="#E5E7EB" BorderThickness="1">
                <DataGrid ItemsSource="{Binding FilteredProducts}" AutoGenerateColumns="False" CanUserAddRows="False" Background="Transparent" RowBackground="White" AlternatingRowBackground="#F8F9FA" BorderThickness="0" GridLinesVisibility="None" HeadersVisibility="Column" SelectionMode="Single">
                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="#6B7280"/>
                            <Setter Property="FontWeight" Value="Medium"/>
                            <Setter Property="FontSize" Value="11"/>
                            <Setter Property="Padding" Value="15,12"/>
                            <Setter Property="RenderTransform"> <!-- Simulation of uppercase letterspacing -->
                                <Setter.Value><ScaleTransform ScaleX="1.06"/></Setter.Value> 
                            </Setter>
                        </Style>
                        <Style TargetType="DataGridRow">
                            <Setter Property="MinHeight" Value="52"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="#E5E7EB"/>
                        </Style>
                    </DataGrid.Resources>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="CATEGORY" Binding="{Binding Category}" Width="150" IsReadOnly="True">
                             <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock"><Setter Property="VerticalAlignment" Value="Center"/> <Setter Property="Margin" Value="15,0"/> <Setter Property="Foreground" Value="#6B7280"/></Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        
                        <DataGridTextColumn Header="PRODUCT NAME" Binding="{Binding Name}" Width="*" IsReadOnly="True">
                             <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock"><Setter Property="VerticalAlignment" Value="Center"/> <Setter Property="Foreground" Value="#111827"/></Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="COST PRICE" Binding="{Binding CostPrice, Converter={StaticResource PriceDashConv}}" Width="120" IsReadOnly="True">
                             <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock"><Setter Property="VerticalAlignment" Value="Center"/> <Setter Property="Foreground" Value="#6B7280"/></Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="SELLING PRICE" Binding="{Binding SellingPrice, StringFormat=Rs {0:N0}}" Width="120" IsReadOnly="True">
                             <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock"><Setter Property="VerticalAlignment" Value="Center"/> <Setter Property="FontWeight" Value="Bold"/> <Setter Property="Foreground" Value="#111827"/></Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="STOCK" Binding="{Binding Quantity}" Width="100" IsReadOnly="True">
                             <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock"><Setter Property="VerticalAlignment" Value="Center"/> <Setter Property="FontWeight" Value="Bold"/> <Setter Property="Foreground" Value="#111827"/></Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTemplateColumn Header="ACTIONS" Width="160">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <Button Content="Edit" Command="{Binding DataContext.OpenEditModalCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Background="Transparent" Foreground="#10A574" BorderBrush="#10A574" BorderThickness="1" Padding="15,6" Margin="0,0,8,0" Cursor="Hand">
                                            <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="6"/></Style></Button.Resources>
                                        </Button>
                                        <Button Content="Delete" Command="{Binding DataContext.OpenDeleteConfirmCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Visibility="{Binding Quantity, Converter={StaticResource StockDelVis}}" Background="#E24B4A" Foreground="White" BorderThickness="0" Padding="15,6" Cursor="Hand">
                                            <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="6"/></Style></Button.Resources>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>

        <!-- Modals Layer -->
        <!-- Edit Product Modal -->
        <Border Background="#66000000" Visibility="{Binding IsEditModalOpen, Converter={StaticResource BoolToVis}}">
            <Border Background="White" CornerRadius="12" Width="480" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="30">
                <StackPanel>
                    <TextBlock Text="Edit Product Details" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,25"/>
                    
                    <TextBlock Text="PRODUCT NAME" FontSize="11" FontWeight="Bold" Foreground="#6B7280" Margin="0,0,0,8"/>
                    <TextBox Text="{Binding SelectedProduct.Name, UpdateSourceTrigger=PropertyChanged}" Padding="15,12" BorderBrush="#E5E7EB" Margin="0,0,0,20"/>

                    <TextBlock Text="SELLING PRICE" FontSize="11" FontWeight="Bold" Foreground="#6B7280" Margin="0,0,0,8"/>
                    <Grid Margin="0,0,0,25">
                         <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                         <Border Background="#F9FAFB" BorderBrush="#E5E7EB" BorderThickness="1,1,0,1" CornerRadius="8,0,0,8" Padding="15,0">
                             <TextBlock Text="Rs" VerticalAlignment="Center" Foreground="#6B7280"/>
                         </Border>
                         <TextBox Grid.Column="1" Text="{Binding SelectedProduct.SellingPrice}" Padding="15,12" BorderBrush="#E5E7EB" BorderThickness="0,1,1,1">
                             <TextBox.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="0,8,8,0"/></Style></TextBox.Resources>
                         </TextBox>
                    </Grid>

                    <Separator Background="#E5E7EB" Margin="0,0,0,20"/>

                    <!-- Read Only Reference Info -->
                    <Grid Margin="0,0,0,25">
                         <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                         <StackPanel Grid.Column="0">
                             <TextBlock Text="CATEGORY" FontSize="10" Foreground="#9CA3AF" FontWeight="Bold"/>
                             <TextBlock Text="{Binding SelectedProduct.Category}" FontSize="13" Foreground="#111827"/>
                         </StackPanel>
                         <StackPanel Grid.Column="1">
                             <TextBlock Text="COST PRICE" FontSize="10" Foreground="#9CA3AF" FontWeight="Bold"/>
                             <TextBlock Text="{Binding SelectedProduct.CostPrice, StringFormat=Rs {0:N0}}" FontSize="13" Foreground="#111827"/>
                         </StackPanel>
                         <StackPanel Grid.Column="2">
                             <TextBlock Text="STOCK" FontSize="10" Foreground="#9CA3AF" FontWeight="Bold"/>
                             <TextBlock Text="{Binding SelectedProduct.Quantity}" FontSize="13" Foreground="#111827"/>
                         </StackPanel>
                    </Grid>

                    <Button Command="{Binding SaveChangesCommand}" Content="SAVE CHANGES" Background="#10A574" Foreground="White" FontWeight="Bold" Padding="0,15" BorderThickness="0" Cursor="Hand" Margin="0,0,0,10">
                         <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="8"/></Style></Button.Resources>
                    </Button>
                    <Button Command="{Binding CloseModalsCommand}" Content="CANCEL" Background="Transparent" Foreground="#6B7280" FontWeight="Medium" BorderThickness="0" Cursor="Hand">
                         <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="8"/></Style></Button.Resources>
                    </Button>
                </StackPanel>
            </Border>
        </Border>

        <!-- Delete Confirmation Overlay -->
        <Border Background="#66000000" Visibility="{Binding IsDeleteConfirmOpen, Converter={StaticResource BoolToVis}}">
            <Border Background="White" CornerRadius="12" Width="380" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="30">
                <StackPanel>
                    <TextBlock Text="{Binding SelectedProduct.Name, StringFormat=Delete {0}?}" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,12" HorizontalAlignment="Center"/>
                    <TextBlock Text="This cannot be undone." FontSize="14" Foreground="#6B7280" Margin="0,0,0,25" HorizontalAlignment="Center"/>
                    
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button Command="{Binding DeleteConfirmedCommand}" Content="DELETE" Background="#E24B4A" Foreground="White" FontWeight="Bold" Padding="25,10" BorderThickness="0" Margin="0,0,10,0"/>
                        <Button Command="{Binding CloseModalsCommand}" Content="CANCEL" Background="Transparent" Foreground="#6B7280" FontWeight="Medium" Padding="25,10" BorderThickness="0"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Border>
    </Grid>
</UserControl>
